# Quick and dirty way to call nestjs from js


```
npm install
npm run build
node src/server.ts
```

```
curl -Lv 'http://localhost:3000/' #plain nestjs
curl -Lv 'http://localhost:3000/legacy' # legacystuff, also doing rabbitmq magic

```
